id: R-DEPLOY-003
when:
  trigger: kubernetes service sends traffic to a pod with no readiness probe configured
  context:
    - pod Running but serving errors during startup
do:
  directive: Add a readinessProbe distinct from liveness so endpoints only include ready pods
  because: Without readiness gating, traffic hits instances still warming up
confidence: 0.88
provenance:
  source_trajectory: packs/pack-deploy/R-DEPLOY-003
  extracted_by: pack-author
  extract_timestamp: "2026-09-01T00:00:00Z"
  extraction_pass: 1
  promotion_commit: builtin
  promotion_mode: builtin
status: active
promoted_at: "2026-09-01T00:00:00Z"
hit_count: 0
tags:
  - deploy
  - kubernetes
  - probes
taxonomy: tool/network
pack: pack-deploy
