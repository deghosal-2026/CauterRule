id: R-DOCKER-001
when:
  trigger: docker build invalidates layer cache by copying source before installing dependencies
  context:
    - Dockerfile with COPY . . before RUN pip install
do:
  directive: Reorder the Dockerfile to copy requirements first, install, then copy source
  because: Dependency layers stay cached across source edits, cutting rebuild time
confidence: 0.9
provenance:
  source_trajectory: packs/pack-docker/R-DOCKER-001
  extracted_by: pack-author
  extract_timestamp: "2026-09-01T00:00:00Z"
  extraction_pass: 1
  promotion_commit: builtin
  promotion_mode: builtin
status: active
promoted_at: "2026-09-01T00:00:00Z"
hit_count: 0
tags:
  - docker
  - build
  - cache
taxonomy: docker/build/cache
pack: pack-docker
